name: do_an
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.1
  flutter_tts: ^4.2.2
  url_launcher: ^6.1.10 # Thêm để mở link
  audioplayers: ^6.0.0 # Thêm để phát file audio

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Khai báo thư mục chứa file audio
  assets:
    - assets/audio/
